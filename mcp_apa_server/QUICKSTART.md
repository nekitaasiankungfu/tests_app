# 🚀 Быстрый старт - MCP сервер APA Search

## Установка за 3 шага

### Шаг 1: Установите зависимости

**Windows:**
```bash
cd mcp_apa_server
install.bat
```

**macOS/Linux:**
```bash
cd mcp_apa_server
chmod +x install.sh
./install.sh
```

Или вручную:
```bash
cd mcp_apa_server
pip install -r requirements.txt
```

### Шаг 2: Настройте Cursor

1. Откройте **Cursor Settings** (Ctrl+,)
2. Найдите раздел **MCP Servers**
3. Нажмите **Edit Config**
4. Вставьте конфигурацию из файла `cursor_config_example.json`
5. **Важно:** Проверьте, что путь к `server.py` правильный!

### Шаг 3: Перезапустите Cursor

Закройте и откройте Cursor заново.

## ✅ Проверка работы

Запустите тестовый скрипт:

```bash
cd mcp_apa_server
python test_server.py
```

Если все тесты пройдены ✅ - сервер готов к работе!

## 💬 Первый запрос

В Cursor напишите:

```
Найди информацию о психологических тестах на депрессию
```

Сервер автоматически найдет релевантную информацию на сайте APA!

## 📚 Больше примеров

Смотрите файл `EXAMPLES.md` для множества примеров запросов.

## ❓ Проблемы?

Читайте полную инструкцию в `README.md`

---

**Готово! Теперь у вас есть доступ ко всей базе знаний APA прямо в Cursor! 🧠✨**

