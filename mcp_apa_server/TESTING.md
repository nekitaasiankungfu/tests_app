# Как проверить, что MCP сервер работает

## 🔍 Метод 1: Диагностика системы

Запустите скрипт диагностики:

```bash
cd mcp_apa_server
diagnose.bat          # Windows
./diagnose.sh         # Linux/Mac
```

Скрипт проверит:
- ✅ Установлен ли Python
- ✅ Установлены ли зависимости
- ✅ Есть ли конфигурация Cursor
- ✅ Работает ли подключение к APA.org

## 🔍 Метод 2: Ручной запуск сервера

### Windows:
```bash
cd c:\Users\bigslainy\psycho\0.0\mcp_apa_server
python server.py
```

### Linux/Mac:
```bash
cd ~/psycho/0.0/mcp_apa_server
python3 server.py
```

**Что должно произойти:**
- Сервер запустится и будет ждать ввода
- Консоль станет "тихой" - это нормально!
- MCP сервер работает через stdio (стандартный ввод/вывод)

⚠️ **НЕ закрывайте** окно консоли - сервер работает в фоне

## 🔍 Метод 3: Проверка в Cursor

### Шаг 1: Настройте Cursor

1. Откройте Cursor
2. Нажмите `Ctrl+,` (Settings)
3. Найдите раздел **"MCP Servers"** или **"Model Context Protocol"**
4. Нажмите **"Edit Config"** или **"Edit MCP Settings"**

### Шаг 2: Добавьте конфигурацию

Вставьте в файл конфигурации:

```json
{
  "mcpServers": {
    "apa-search": {
      "command": "python",
      "args": [
        "C:\\Users\\bigslainy\\psycho\\0.0\\mcp_apa_server\\server.py"
      ],
      "env": {},
      "disabled": false
    }
  }
}
```

**ВАЖНО:** Используйте двойные обратные слеши `\\` в Windows путях!

### Шаг 3: Перезапустите Cursor

Полностью закройте и откройте Cursor заново.

### Шаг 4: Проверьте подключение

#### Способ A: Через Developer Tools

1. В Cursor нажмите `Ctrl+Shift+I` (DevTools)
2. Перейдите на вкладку **Console**
3. Ищите сообщения об MCP серверах:
   - `[MCP] Connected to apa-search` ✅ - работает
   - `[MCP] Failed to connect` ❌ - ошибка

#### Способ B: Через чат с AI

Откройте чат в Cursor и напишите:

```
Используй MCP сервер apa-search и найди информацию о психологических тестах на депрессию
```

**Признаки успешной работы:**
- ✅ AI начнет поиск на APA.org
- ✅ Вы увидите результаты с сайта APA
- ✅ В ответе будут ссылки на www.apa.org

**Признаки проблем:**
- ❌ AI скажет "Я не могу получить доступ к интернету"
- ❌ AI не использует MCP инструменты
- ❌ Ошибка в консоли DevTools

## 🔍 Метод 4: Проверка логов Cursor

### Windows:
```
%APPDATA%\Cursor\logs\
```

### Mac:
```
~/Library/Application Support/Cursor/logs/
```

### Linux:
```
~/.config/Cursor/logs/
```

Откройте последний лог-файл и ищите:
- `apa-search` - упоминания вашего сервера
- `stderr` - ошибки запуска
- `spawn` - попытки запуска процесса

## ✅ Контрольный список

Пройдите по этому списку:

### Python установлен?
```bash
python --version
```
Должно показать: `Python 3.10.x` или выше

### Зависимости установлены?
```bash
cd mcp_apa_server
pip list | findstr mcp
pip list | findstr httpx
pip list | findstr beautifulsoup4
```

Все три пакета должны быть в списке.

### Конфигурация правильная?

Проверьте файл конфигурации Cursor:
- Путь к `server.py` правильный?
- Используются двойные обратные слеши `\\`?
- Команда `python` работает в терминале?

### Cursor перезапущен?

После изменения конфигурации **ОБЯЗАТЕЛЬНО** перезапустите Cursor.

## 🐛 Частые проблемы

### 1. Python не найден

**Решение:**
1. Установите Python 3.10+ с https://www.python.org/downloads/
2. При установке отметьте **"Add Python to PATH"**
3. Перезагрузите терминал

### 2. Зависимости не установлены

**Решение:**
```bash
cd mcp_apa_server
pip install -r requirements.txt
```

### 3. Cursor не видит сервер

**Решение:**
1. Проверьте путь в конфигурации (используйте полный абсолютный путь)
2. Убедитесь, что `python` доступен из командной строки
3. Перезапустите Cursor
4. Проверьте логи Cursor (см. Метод 4)

### 4. Сервер запускается, но не отвечает

**Решение:**
1. Проверьте интернет-соединение
2. Попробуйте открыть https://www.apa.org в браузере
3. Проверьте настройки прокси/firewall

### 5. "Permission denied" на Linux/Mac

**Решение:**
```bash
chmod +x mcp_apa_server/server.py
chmod +x mcp_apa_server/install.sh
```

## 📊 Ожидаемое поведение

### При успешной работе:

1. **В Cursor DevTools** (F12):
   ```
   [MCP] Starting server: apa-search
   [MCP] Server apa-search connected
   [MCP] Tools available: 4
   ```

2. **В чате AI**:
   - AI использует инструменты `search_apa_site`, `get_apa_page_content` и т.д.
   - Появляются результаты с сайта APA.org
   - Ссылки ведут на www.apa.org

3. **При прямом запуске** `python server.py`:
   - Консоль "зависнет" и будет ждать - это нормально!
   - Сервер работает в режиме stdio

## 🎯 Быстрый тест

Выполните этот тест, чтобы убедиться, что все работает:

```bash
# 1. Перейдите в директорию
cd c:\Users\bigslainy\psycho\0.0\mcp_apa_server

# 2. Запустите диагностику
diagnose.bat

# 3. Если все OK, откройте Cursor и напишите в чате:
# "Найди на APA.org информацию о валидации психологических тестов"
```

Если AI найдет и покажет результаты с APA.org - **сервер работает!** ✅

---

Нужна помощь? Проверьте логи или запустите диагностику!

