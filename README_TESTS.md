# 🧪 Быстрый старт: Запуск тестов

## ⚡ Самый быстрый способ

### Windows (PowerShell или CMD)

```bash
# Перейдите в папку проекта
cd D:\Work\psycho_app\0.0.1.x_dev

# Запустите тесты
flutter test test/axis_directions_test.dart
```

### Альтернативный способ (через bat-файл)

Просто **двойной клик** на файл `run_axis_tests.bat`

---

## 📊 Что должно получиться

Вы должны увидеть примерно такой вывод:

```
00:00 +0: loading test/axis_directions_test.dart
00:01 +1: axisDirections - Базовая логика инверсии QuestionWeight.getDirection() возвращает 1 по умолчанию
00:01 +2: axisDirections - Базовая логика инверсии QuestionWeight.getDirection() возвращает -1 для инвертированных осей
00:01 +3: axisDirections - ScaleAccumulator инверсия Прямое направление (direction=1): высокий балл → высокая шкала
...
00:03 +16: All tests passed!
```

**Ожидаемый результат:** ✅ **All tests passed!**

---

## ❌ Что делать если тесты не проходят

### Ошибка компиляции

```
Error: Cannot find import '...'
```

**Решение:**
```bash
flutter pub get
flutter clean
flutter pub get
flutter test test/axis_directions_test.dart
```

### Тест провалился

```
Expected: <100.0>
  Actual: <0.0>
```

**Это серьезная ошибка!** Значит система инверсии не работает.

**Действия:**
1. Откройте issue с текстом ошибки
2. Скопируйте полный вывод теста
3. Укажите, какой именно тест упал

---

## 📝 Полная документация

Подробная документация доступна в файле:
**`TESTING_AXIS_DIRECTIONS.md`**

---

## 🎯 Запуск только одной группы тестов

```bash
# Только тесты стресса
flutter test test/axis_directions_test.dart --name "Стресс тест"

# Только базовая логика
flutter test test/axis_directions_test.dart --name "Базовая логика"

# Только Love Profile
flutter test test/axis_directions_test.dart --name "Love Profile"
```

---

## 🚀 Запуск ВСЕХ тестов проекта

```bash
flutter test
```

Это запустит все тесты, включая `axis_directions_test.dart`

---

## 📞 Нужна помощь?

1. Прочитайте `TESTING_AXIS_DIRECTIONS.md`
2. Проверьте, что все файлы на месте
3. Запустите `flutter doctor` для проверки окружения
