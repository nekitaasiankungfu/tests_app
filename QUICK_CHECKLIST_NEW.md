# 🚀 QUICK CHECKLIST - Добавление нового теста

> **Версия:** 3.0
> **Обязателен к прочтению перед добавлением теста!**

---

## ФАЗА 1: ПОДГОТОВКА (5 пунктов)

□ **1. Прочитать все 8 ПРАВИЛ** в разделе "Критические правила"
   - Особое внимание: ПРАВИЛО #2 (отрицательные веса), ПРАВИЛО #6 (только существующие шкалы), ПРАВИЛО #8 (maxQuestionScore)

□ **2. Выбрать референсный тест для копирования**
   - ✅ **ИСПОЛЬЗУЙТЕ:** `burnout_diagnostic_v1` или `social_battery_v1` (актуальный API)
   - ❌ **НЕ ИСПОЛЬЗУЙТЕ:** старые тесты (sixteen_types, ipip_big_five) - устаревший API!

□ **3. Определить параметры теста**
   - Test ID (например: `emotional_intelligence_test`)
   - Категория (только 4 существующих: personality, temperament, relationships, emotional)
   - Количество вопросов
   - Шкала ответов (0-3, 0-4, или 0-5)

□ **4. Проверить что категория существует**
   - Открыть `lib/models/test_category.dart`
   - Найти вашу категорию в списке (personality, temperament, relationships, emotional)
   - Если нужна новая категория - сначала добавить её в TestCategory

□ **5. Подготовить спецификацию теста**
   - Список всех вопросов (текст на русском и английском)
   - Факторы/шкалы теста (уникальные для данного теста)
   - Интерпретации для каждого фактора (low/medium/high)

---

## ФАЗА 2: СОЗДАНИЕ ФАЙЛОВ (20 пунктов)

### Test Stub (lib/data/tests/)

□ **6. Создать test stub файл**
   - Путь: `lib/data/tests/your_test_test.dart`
   - Скопировать структуру из `burnout_diagnostic_test.dart`

□ **7. Заполнить метаданные теста**
   - `id` - уникальный ID теста
   - `category` - ТОЛЬКО одна из 4 существующих категорий ⚠️
   - `name` - название на русском и английском
   - `description` - описание на русском и английском

□ **8. Проверить что НЕ используется несуществующая категория** ⚠️
   - Симптом ошибки: тест не появляется на главном экране
   - Решение: использовать только 'personality', 'temperament', 'relationships', 'emotional'

□ **9. Указать параметры теста**
   - `questionCount` - количество вопросов
   - `estimatedMinutes` - примерное время прохождения
   - `type` - 'simple' или 'multi_factor'
   - `assetPath` - оставить пустым `''` (используем Legacy Dart)

□ **10. Добавить metadata**
   - `version`, `author`, `year`
   - `category_display` - отображаемое название категории
   - `factors` - список ID факторов (если multi_factor)
   - `response_type` - тип ответов (likert_3, likert_5, etc.)

### Data File (lib/data/)

□ **11. Создать data файл**
   - Путь: `lib/data/your_test_data.dart`
   - ✅ **КОПИРОВАТЬ** из `burnout_diagnostic_data.dart` (актуальный API!)
   - ❌ **НЕ копировать** из старых тестов!

□ **12. Проверить что НЕ используется FactorModel** ⚠️
   - Симптом ошибки: "Undefined class: 'FactorModel'"
   - FactorModel - это устаревший API, НЕ существует в текущей версии!
   - Решение: использовать `getFactorNames()` и `getFactorInterpretation()`

□ **13. Проверить правильную структуру текстовых полей** ⚠️
   - ✅ **ПРАВИЛЬНО:** `title: {'ru': '...', 'en': '...'}`
   - ❌ **НЕПРАВИЛЬНО:** `titleKey: 'test.title'`
   - ✅ **ПРАВИЛЬНО:** `text: {'ru': '...', 'en': '...'}`
   - ❌ **НЕПРАВИЛЬНО:** `textKey: 'question.1.text'`

□ **14. Создать метод getYourTestTest()**
   - Возвращает `TestModel` с заполненными полями
   - `id`, `title`, `description`, `categoryId`, `factorIds`, `questions`, `type`

□ **15. Заполнить все вопросы (QuestionModel)**
   - `id` - уникальный ID вопроса (q1, q2, ...)
   - `text` - Map<String, String> с текстом на ru и en
   - `answers` - список AnswerModel
   - `factorId` - ID фактора (если multi_factor)
   - `isReversed` - если вопрос обратный

□ **16. КРИТИЧНО: Проверить scores в AnswerModel** ⚠️
   - **ПРАВИЛО #1:** Scores ДОЛЖНЫ быть 0-4 (НЕ 1-5!)
   - Проблема scores 1-5: минимум будет 20% вместо 0%
   - Каждый AnswerModel: `score: 0, 1, 2, 3, 4` (не 1, 2, 3, 4, 5!)
   - Добавить обязательный параметр `id` в каждый AnswerModel

□ **17. Создать метод getFactorNames()**
   - Возвращает `Map<String, Map<String, String>>`
   - Пример: `{'factor_id': {'ru': 'Название', 'en': 'Name'}}`

□ **18. Создать метод getFactorInterpretation()**
   - Параметры: `(String factorId, double percentage)`
   - Возвращает `Map<String, String>` с интерпретацией на ru и en
   - Добавить NaN-защиту: `if (percentage.isNaN) percentage = 0.0;`
   - 3 уровня интерпретации: low (0-40%), medium (40-70%), high (70-100%)

□ **19. Проверить правильность именования класса** ⚠️
   - ✅ **ПРАВИЛЬНО:** `class YourTestData` (без дублирования "Test")
   - ❌ **НЕПРАВИЛЬНО:** `class YourTestTestData` или `class YourTest TestData`
   - Симптом ошибки: ошибки компиляции в 5+ файлах

### Weights File (lib/config/summary/question_weights/)

□ **20. Создать weights файл**
   - Путь: `lib/config/summary/question_weights/your_test_weights.dart`
   - Название класса: `YourTestWeights` (без "Test" дублирования)

□ **21. Импортировать QuestionWeight**
   - `import 'question_weight_models.dart';`

□ **22. Создать Map весов**
   - `static final Map<String, QuestionWeight> weights = { ... }`
   - Ключи: `'test_id:q1'`, `'test_id:q2'`, etc.

□ **23. Для каждого вопроса создать QuestionWeight**
   - `testId` - ID теста
   - `questionId` - ID вопроса (q1, q2, ...)
   - `axisWeights` - Map<String, double> весов на шкалы

□ **24. КРИТИЧНО: Использовать ТОЛЬКО существующие шкалы** ⚠️
   - **ПРАВИЛО #6:** Проверяйте каждую шкалу в `hierarchical_scales.dart`
   - 195 психологических шкал + 8 биполярных полюсов
   - Симптом ошибки: тест показывает "91 шкала" в результатах, но "45 шкал" в Summary
   - Запустить проверку: `bash tools/validate_test.sh your_test_id`

□ **25. Проверить отсутствие дубликатов ключей** ⚠️
   - Симптом ошибки: ошибки компиляции Dart
   - Проверка через grep: ищем повторяющиеся ключи в Map

---

## ФАЗА 3: ИНТЕГРАЦИЯ (25 пунктов)

### Точка интеграции #1: test_registry.dart

□ **26. Добавить import в test_registry.dart**
   - `import 'tests/your_test_test.dart';`

□ **27. Добавить тест в allTests[]**
   - `YourTestTest(),` в список allTests

### Точка интеграции #2: test_loader_service.dart

□ **28. Добавить import в test_loader_service.dart**
   - `import '../data/your_test_data.dart';`

□ **29. Добавить case в _loadLegacyTest()**
   - ```dart
     case 'your_test_id':
       test = YourTestData.getYourTestTest();
       break;
     ```

### Точка интеграции #3: test_service.dart (3 блока!)

□ **30. Добавить import в test_service.dart**
   - `import '../data/your_test_data.dart';`

□ **31. БЛОК 1: Добавить maxQuestionScore (если scores НЕ 0-5)** ⚠️
   - **ПРАВИЛО #8:** Если тест использует scores 0-3 или 0-4
   - Найти метод `calculateResult` (строка ~92)
   - Добавить case:
     ```dart
     final int maxQuestionScore = test.id == 'fisher_temperament'
         ? 3
         : test.id == 'your_test_id'
             ? 4  // Ваш тест uses 0-4
             : 5; // Default: 0-5
     ```
   - Симптом если пропустить: максимальный ответ показывает 80% вместо 100%

□ **32. БЛОК 2: Добавить факторные имена (строка ~222)**
   - ```dart
     } else if (test.id == 'your_test_id') {
       factorNames = YourTestData.getFactorNames();
       factorInterpretations = {};
     ```
   - Симптом если пропустить: "неизвестный фактор" в результатах

□ **33. БЛОК 3: Добавить интерпретации (строка ~266)**
   - ```dart
     } else if (testResult.testId == 'your_test_id') {
       final percentage = (score / maxFactorScore) * 100;
       interpretation = YourTestData.getFactorInterpretation(factorId, percentage);
     ```
   - Симптом если пропустить: "нет интерпретации" в результатах

### Точка интеграции #4: summary_service.dart (2 места!) ⚠️ КРИТИЧНО

□ **34. Добавить import в summary_service.dart**
   - `import '../config/summary/question_weights/your_test_weights.dart';`

□ **35. МЕСТО 1: Добавить веса в allWeights (строка ~125)**
   - ```dart
     final allWeights = <String, QuestionWeight>{
       ...IPIPBigFiveWeights.weights,
       ...YourTestWeights.weights,  // Добавить здесь!
     };
     ```
   - Симптом если пропустить: Personality Type на Summary Screen не показывает влияние теста

□ **36. МЕСТО 2: Добавить веса во второй allWeights (строка ~500)**
   - ```dart
     final allWeights = <String, QuestionWeight>{
       ...IPIPBigFiveWeights.weights,
       ...YourTestWeights.weights,  // И здесь тоже!
     };
     ```
   - Симптом если пропустить: нет текста вопросов при развёртывании шкал

### Точка интеграции #5: summary_screen.dart (2 switch!) ⚠️ КРИТИЧНО

□ **37. Добавить import в summary_screen.dart**
   - `import '../data/your_test_data.dart';`

□ **38. SWITCH 1: Добавить case в _getAnswerText (строка ~69)**
   - ```dart
     case 'your_test_id':
       testModel = YourTestData.getYourTestTest();
       break;
     ```
   - Симптом если пропустить: "Unknown testId" в логах, нет текста ответов

□ **39. SWITCH 2: Добавить case в _getQuestionText (строка ~2569)**
   - ```dart
     case 'your_test_id':
       testModel = YourTestData.getYourTestTest();
       break;
     ```
   - Симптом если пропустить: нет текста вопросов при развёртывании шкал

### Точка интеграции #6: summary_config.dart ⚠️ ОБЯЗАТЕЛЬНО

□ **40. Добавить import в summary_config.dart**
   - `import 'summary/question_weights/your_test_weights.dart';`

□ **41. Добавить веса в QuestionWeightsConfig._weights (строка ~2303)**
   - ```dart
     static final Map<String, QuestionWeight> _weights = {
       ...IPIPBigFiveWeights.weights,
       ...YourTestWeights.weights,  // Добавить здесь!
     };
     ```
   - Симптом если пропустить: Test Result Screen не показывает "Affected Scales: N шкал"

□ **42. Обновить комментарий в summary_config.dart**
   - Добавить строку: `// - your_test_weights.dart (X questions, Y scales)`

---

## ФАЗА 4: ПРОВЕРКИ ПЕРЕД ЗАПУСКОМ (8 пунктов)

□ **43. Запустить автоматическую валидацию**
   - `bash tools/validate_test.sh your_test_id`
   - Скрипт проверяет: валидность шкал, дубликаты, регистрацию во всех 7 точках

□ **44. Проверить отсутствие отрицательных весов на биполярных полюсах** ⚠️
   - **ПРАВИЛО #2:** extraversion, introversion, sensing, intuition, thinking, feeling, judging, perceiving
   - НЕ ДОЛЖНЫ иметь отрицательные веса!
   - Вместо: `'introversion': -1.0` → использовать `'extraversion': 1.0`

□ **45. Проверить что все шкалы существуют**
   - Открыть `lib/config/summary/hierarchical_scales.dart`
   - Ctrl+F для каждой шкалы из weights файла
   - Исключение: 8 биполярных полюсов (они в bipolar_scales.dart)

□ **46. Запустить flutter analyze**
   - `flutter analyze`
   - Должно быть: "No issues found!"
   - Если есть ошибки - исправить перед запуском

□ **47. Проверить что НЕ используется FactorModel** ⚠️
   - Grep: `grep -r "FactorModel" lib/data/your_test_data.dart`
   - Должно быть: 0 результатов

□ **48. Проверить scores 0-4 во всех AnswerModel**
   - Grep: `grep "score: [1-5]" lib/data/your_test_data.dart`
   - Убедиться что scores начинаются с 0 (не с 1!)

□ **49. Сделать git commit перед запуском**
   - На случай если что-то пойдёт не так, можно откатиться

□ **50. FULL RESTART (не hot reload!)**
   - Hot reload НЕ подхватывает все изменения в регистрации
   - Остановить приложение полностью
   - Запустить заново: `flutter run`

---

## ФАЗА 5: ТЕСТИРОВАНИЕ ПОСЛЕ ЗАПУСКА (15 пунктов)

### Главный экран

□ **51. Тест появился на главном экране**
   - Если нет - проверить категорию в test stub

□ **52. Тест находится в правильной категории**
   - Категория соответствует заданной в test stub

□ **53. Название и описание отображаются на обоих языках**
   - Переключить язык (Settings → Language)
   - Проверить что тексты меняются

### Прохождение теста

□ **54. Все вопросы отображаются**
   - Количество совпадает с questionCount

□ **55. Тексты вопросов читабельны на обоих языках**

□ **56. Все варианты ответов отображаются**

□ **57. Прогресс-бар работает корректно**

### Результаты теста

□ **58. Факторы отображаются с названиями (не "неизвестный фактор")** ⚠️
   - Если "неизвестный фактор" - проверить test_service.dart БЛОК 2

□ **59. Интерпретации отображаются (не "нет интерпретации")** ⚠️
   - Если "нет интерпретации" - проверить test_service.dart БЛОК 3

□ **60. Максимальный ответ показывает 100% (не 80%!)** ⚠️
   - Пройти тест отвечая максимально на все вопросы
   - Если показывает 80% - проверить maxQuestionScore в test_service.dart

□ **61. Test Result Screen показывает "Affected Scales: N шкал"**
   - Если "0 шкал" - проверить summary_config.dart

### Summary Screen

□ **62. Тест появился в списке влияющих на психологические шкалы**
   - Открыть Summary Screen
   - Развернуть любую шкалу
   - Найти ваш тест в списке

□ **63. При развёртывании шкалы виден текст вопроса** ⚠️
   - Если "Вопрос q1" вместо текста - проверить summary_screen.dart SWITCH 2

□ **64. При развёртывании шкалы виден текст ответа** ⚠️
   - Должно быть "2/4 (Иногда)" вместо просто "2"
   - Если только цифра - проверить summary_screen.dart SWITCH 1

□ **65. Personality Type биполярные шкалы показывают влияние теста**
   - Открыть Summary Screen → Personality Type (E/I, S/N, T/F, J/P)
   - Развернуть шкалу → проверить что ваш тест в списке
   - Если нет - проверить summary_service.dart (оба места!)

---

## ✅ CHECKLIST ЗАВЕРШЁН!

Если все 65 пунктов пройдены - тест успешно добавлен!

**Следующие шаги:**
1. Создать git commit с описанием теста
2. Обновить CHANGELOG.md
3. (Опционально) Запустить полное тестирование приложения

**В случае проблем:**
- Обратитесь к разделу "Типичные ошибки" в соответствующем шаге
- Запустите `bash tools/validate_test.sh your_test_id` для диагностики
- Проверьте что следовали ВСЕМ 8 правилам
