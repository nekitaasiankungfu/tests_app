# Быстрый старт: Общее саммари

## Что добавлено

✅ **6 шкал личности**: Эмоциональный баланс, Самоконтроль, Социальность, Осознанность, Мотивация, Креативность
✅ **Визуализация**: Радар-диаграмма (паутина) + мини-шкалы
✅ **Умные расчёты**: Взвешивание, нормализация, определение уверенности
✅ **Локализация**: Полная поддержка русского и английского
✅ **Интеграция**: Бесшовное добавление в существующий экран результатов

## Как использовать

### 1. Доступ к функции
- Перейдите в раздел "Результаты"
- Если пройдено ≥2 тестов, увидите кнопку "Саммари" и карточку "Общее саммари"
- Нажмите для просмотра персонального отчёта

### 2. В коде (для разработчиков)

```dart
// Получить провайдер
final summaryProvider = Provider.of<SummaryProvider>(context);

// Рассчитать саммари на основе всех результатов
await summaryProvider.refreshSummary(testProvider, languageCode);

// Проверить доступность
if (summaryProvider.hasSummary) {
  final summary = summaryProvider.currentSummary!;
  final axisScores = summaryProvider.getAllAxisScores();
  final strengths = summaryProvider.getStrengths();
  final areas = summaryProvider.getDevelopmentAreas();
}
```

### 3. Структура данных

```dart
// Основные данные саммари
SummaryData {
  axisScores: Map<String, SummaryScore>  // Баллы по шкалам
  confidence: ConfidenceLevel            // Уровень уверенности
  summaryText: String                    // Текстовое описание
  advice: List<String>                   // Советы
  strengths: List<String>               // Сильные стороны
  developmentAreas: List<String>        // Зоны развития
  testInfluences: List<TestInfluence>   // Влияние тестов
}

// Баллы по шкале
SummaryScore {
  axisId: String      // ID шкалы
  score: double       // Нормализованный балл 0-100
  confidence: double  // Уверенность в балле 0-1
  contributions: List<TestContribution> // Вклады от тестов
}
```

## Ключевые особенности

### 🎯 Умное соответствие тестов
- **Big Five**: Эмоциональная стабильность → Эмоциональный баланс (инвертировано)
- **Fisher Temperament**: Осторожность → Самоконтроль, Просоциальность → Социальность
- **Стресс тест**: Общий балл → Эмоциональный баланс (инвертировано)
- **Самооценка**: Общий балл → Осознанность (0.8), Мотивация (0.5)

### 📊 Визуализация
- **Радар-диаграмма**: 6-угольная паутина с цветовой кодировкой
- **Мини-шкалы**: Горизонтальные полосы с процентами
- **Цвета**: Зеленый (высокий), Оранжевый (средний), Красный (низкий)

### 🎭 Уверенность профиля
- **Высокая**: ≥3 разных тестов
- **Средняя**: ≥2 тестов
- **Низкая**: <2 тестов

### 🌍 Локализация
- Все тексты на русском и английском
- Автоматическое переключение по языку приложения
- Дружественный тон без клинических терминов

## Файлы, которые добавлены/изменены

### Новые файлы:
- `lib/config/summary_config.dart` - Конфигурация шкал и соответствий
- `lib/models/summary_model.dart` - Модели данных и калькулятор
- `lib/providers/summary_provider.dart` - Управление состоянием
- `lib/widgets/summary_charts.dart` - Визуализация (радар + шкалы)
- `lib/screens/summary_screen.dart` - Основной экран саммари
- `lib/config/SUMMARY_README.md` - Подробная документация
- `test/summary_test.dart` - Тесты функционала

### Изменённые файлы:
- `lib/main.dart` - Добавлен SummaryProvider
- `lib/screens/results_screen.dart` - Интеграция саммари
- `lib/providers/test_provider.dart` - Уведомления о изменениях

## Запуск и тестирование

1. **Соберите приложение**: `flutter build apk` или `flutter run`
2. **Пройдите тесты**: Минимум 2 разных теста (Big Five + любой другой)
3. **Откройте саммари**: Результаты → Саммари
4. **Проверьте**: Визуализация, текст, советы, уверенность

## Расширение функционала

### Добавление новых тестов:
1. В `summary_config.dart` добавьте соответствия в `testMappings`
2. Укажите веса и направления для каждой шкалы
3. Обновите тесты в `test/summary_test.dart`

### Добавление новых шкал:
1. В `SummaryConfig.axes` добавьте новую шкалу
2. Укажите цвет, иконку, описание
3. Обновите соответствия тестов

### Кастомизация визуализации:
1. Измените `summary_charts.dart` для новых видов диаграмм
2. Добавьте новые типы шкал в `MiniScale` и `GradientScale`
3. Настройте цвета и стили в `SummaryVisualization`

## Технические детали

- **Нормализация**: Линейное преобразование к шкале 0-100
- **Взвешивание**: Учёт весов и направлений из конфигурации
- **Хранение**: JSON в SharedPreferences (анонимно)
- **Производительность**: Расчёт при изменении результатов
- **Память**: Автоматическая очистка при удалении результатов

## Поддержка

При проблемах проверьте:
1. Минимум 2 пройденных теста
2. Корректность результатов в TestProvider
3. Правильность языка в LocaleProvider
4. Логи в консоли при отладке
