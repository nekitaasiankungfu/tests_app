### 🔴 ПРАВИЛО #2: Отрицательные веса - когда и как использовать

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО:** Отрицательные веса имеют РАЗНЫЕ правила для биполярных и обычных шкал!

#### Таблица правил использования отрицательных весов

| Тип шкалы | Количество | Отрицательные веса | Что делать вместо | Пример |
|-----------|-----------|-------------------|-------------------|--------|
| **195 психологических шкал** (hierarchical_scales.dart) | 195 шкал | ✅ **ДА** - для обратных корреляций | Используйте отрицательный вес | `'vitality': -1.0` → высокая усталость = низкая энергия |
| **8 биполярных полюсов** (Personality Type) | 8 полюсов | ❌ **НЕТ** - используйте противоположный полюс | Положительный вес на другом полюсе | `'extraversion': -0.3` → `'introversion': 0.3` |

#### 8 биполярных Personality Type полюсов (НЕ используйте отрицательные веса!)

| Биполярная шкала | Полюс 1 | Полюс 2 | Правило инверсии |
|-----------------|---------|---------|------------------|
| personality_type_ei | extraversion | introversion | `'extraversion': -0.3` → `'introversion': 0.3` |
| personality_type_sn | sensing | intuition | `'sensing': -0.4` → `'intuition': 0.4` |
| personality_type_tf | thinking | feeling | `'thinking': -0.5` → `'feeling': 0.5` |
| personality_type_jp | judging | perceiving | `'judging': -0.3` → `'perceiving': 0.3` |

**Почему нельзя?** Биполярные шкалы - это оси с двумя противоположными полюсами. Отрицательный вес на одном полюсе = положительный вес на противоположном полюсе.

#### Когда использовать отрицательные веса (195 психологических шкал)

**Отрицательные веса показывают ОБРАТНУЮ КОРРЕЛЯЦИЮ:**
- Высокий ответ на вопрос = НИЗКИЙ балл по шкале
- Пример: "Как часто вы чувствуете усталость?" (ответ: часто = 100%)
  - `'vitality': -1.0` → высокая усталость = НИЗКАЯ vitality (0%)
  - `'stress_tolerance': -0.8` → высокая усталость = НИЗКАЯ стрессоустойчивость (20%)
  - `'burnout': 0.7` → высокая усталость = ВЫСОКОЕ выгорание (70%)

#### Правило комментариев "(reversed)"

**⚠️ ОБЯЗАТЕЛЬНО:** Все шкалы с комментариями "(reversed)" ДОЛЖНЫ иметь отрицательные веса!

```dart
// ✅ ПРАВИЛЬНО:
'burnout_diagnostic_v1:q12': QuestionWeight(
  axisWeights: {
    'vitality': -1.0,                // (reversed) - ОТРИЦАТЕЛЬНЫЙ
    'emotional_resilience': -0.9,    // (reversed) - ОТРИЦАТЕЛЬНЫЙ
    'stress_tolerance': -0.8,        // (reversed) - ОТРИЦАТЕЛЬНЫЙ
    'burnout': 0.7,                  // Прямая связь - ПОЛОЖИТЕЛЬНЫЙ
  },
),

// ❌ НЕПРАВИЛЬНО:
'burnout_diagnostic_v1:q12': QuestionWeight(
  axisWeights: {
    'vitality': 1.0,                 // ❌ Комментарий "(reversed)", но вес положительный!
    'stress_tolerance': 0.8,         // ❌ Будет показывать 80% вместо 20%!
  },
)
```

#### Типы обратной корреляции

**1. Дефицит ресурса (низкая способность):**
```dart
'attention_control': -1.0,       // Трудно сфокусироваться = низкий контроль
'working_memory': -0.8,          // Плохая память = низкая рабочая память
'stress_tolerance': -0.7,        // Высокий стресс = низкая толерантность
'emotional_resilience': -0.9,    // Высокая уязвимость = низкая устойчивость
'vitality': -1.0,                // Высокая усталость = низкая энергия
```

**2. Отсутствие позитивных качеств:**
```dart
'self_compassion': -1.0,         // Высокая самокритика = низкое самосострадание
'self_esteem': -0.9,             // Низкая самооценка = отрицательная самооценка
'composure': -0.7,               // Высокая раздражительность = низкое спокойствие
'calmness': -0.6,                // Высокое напряжение = низкое спокойствие
```

**3. Обратная зависимость:**
```dart
'self_consciousness': -0.9,      // Высокая уверенность = низкое самосознание
'anxiety': -0.5,                 // Высокая уверенность = низкая тревога
'secure_attachment': -0.7,       // Высокое сравнение = низкая безопасность
```

#### Математика отрицательных весов

**Пример:** Вопрос "Как часто усталость?" - ответ 100% (максимум)

**С положительным весом (НЕПРАВИЛЬНО для reversed):**
```dart
'vitality': 1.0  // ОШИБКА!
score = 1.0 × 1.0 = 1.0 → 100%  // ❌ Высокая усталость = высокая энергия?!
```

**С отрицательным весом (ПРАВИЛЬНО для reversed):**
```dart
'vitality': -1.0  // ✅
score = 1.0 × (-1.0) = -1.0 → инверсия → 0%  // ✅ Высокая усталость = низкая энергия
```

#### Проверка правильности весов

**После создания weights файла выполните:**

```bash
# Проверка на наличие положительных весов с "(reversed)" комментариями
grep "': [0-9].*// .*(reversed)" lib/config/summary/question_weights/your_test_weights.dart
```

**Ожидаемый результат:** 0 совпадений (все "(reversed)" должны иметь отрицательные веса)

**Если находятся совпадения:** Замените положительные веса на отрицательные:
- `0.7` → `-0.7`
- `1.0` → `-1.0`
- `0.5` → `-0.5`

#### Примеры ПРАВИЛЬНОГО использования

```dart
// ✅ 195 психологических шкал - отрицательные веса OK
'burnout_diagnostic_v1:q5': QuestionWeight(
  axisWeights: {
    'vitality': -1.0,           // Усталость снижает энергию
    'stress_tolerance': -0.8,   // Стресс снижает толерантность
    'anxiety': 0.9,             // Стресс повышает тревогу (прямая связь)
  },
),

// ✅ Personality Type - только положительные на полюсах
'burnout_diagnostic_v1:q5': QuestionWeight(
  axisWeights: {
    'introversion': 0.4,        // Усталость связана с интроверсией
    'feeling': 0.3,             // Эмоциональность связана с Feeling
    // НЕ используем 'extraversion': -0.4 или 'thinking': -0.3
  },
),
```

#### Примеры НЕПРАВИЛЬНОГО использования

```dart
// ❌ НЕПРАВИЛЬНО - биполярные полюса с отрицательными весами
'disc_personality_v1:q42': QuestionWeight(
  axisWeights: {
    'introversion': -1.0,  // ❌ Биполярный полюс! Используйте 'extraversion': 1.0
    'feeling': -0.7,       // ❌ Биполярный полюс! Используйте 'thinking': 0.7
  },
),

// ❌ НЕПРАВИЛЬНО - reversed шкала с положительным весом
'burnout_diagnostic_v1:q12': QuestionWeight(
  axisWeights: {
    'vitality': 1.0,  // ❌ Это reversed шкала! Должно быть -1.0
  },
),
```

#### Резюме правил

**✅ ИСПОЛЬЗУЙТЕ отрицательные веса для:**
- 195 психологических шкал (hierarchical_scales.dart)
- Шкал с комментариями "(reversed)"
- Обратных корреляций (высокий вопрос = низкая шкала)

**❌ НЕ используйте отрицательные веса для:**
- 8 биполярных Personality Type полюсов
- Вместо этого: используйте положительный вес на противоположном полюсе

**Автоматическая проверка:**
- Запустите `bash tools/validate_test.sh your_test_id`
- Проверка D: найдёт отрицательные веса на биполярных полюсах

---
