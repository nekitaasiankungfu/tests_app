# 🎯 BIPOLAR WEIGHTS - Руководство и защита от ошибок

> **Критически важно!** Неправильный формат биполярных весов приводит к тому, что Summary Screen НЕ ОТОБРАЖАЕТ влияние теста на 4 биполярные шкалы (E/I, S/N, T/F, J/P).

---

## 🔴 Распространённая ошибка: Отдельные QuestionWeight с суффиксом `:personality`

### ❌ НЕПРАВИЛЬНЫЙ подход (НЕ РАБОТАЕТ!)

```dart
// ❌ Основной вопрос - ТОЛЬКО hierarchical шкалы
'friendship_red_flags_v1:q1': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    'boundary_setting': -1.0,
    'assertiveness': -0.9,
    'self_esteem': -0.7,
  },
),

// ❌ ОТДЕЛЬНЫЙ QuestionWeight для биполярных весов
// Это НЕ РАБОТАЕТ! Summary Screen не увидит эти веса!
'friendship_red_flags_v1:q1:personality': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    'feeling': 0.5,
    'perceiving': 0.3,
  },
),
```

**Почему НЕ работает:**
- `SummaryService` обрабатывает ключи вида `test_id:qN`, а НЕ `test_id:qN:personality`
- Биполярные веса игнорируются
- Summary Screen не показывает влияние на E/I, S/N, T/F, J/P
- Компилируется без ошибок, но НЕ работает в runtime!

---

## ✅ ПРАВИЛЬНЫЙ подход: Встраивать в основной QuestionWeight

```dart
// ✅ Hierarchical И биполярные веса В ОДНОМ QuestionWeight!
'friendship_red_flags_v1:q1': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    // ════════════════════════════════════════
    // 195 hierarchical psychological scales
    // ════════════════════════════════════════
    'boundary_setting': -1.0,
    'assertiveness': -0.9,
    'self_esteem': -0.7,
    'fear_of_abandonment': 0.8,
    'compliance': 0.7,

    // ════════════════════════════════════════
    // 8 bipolar personality type poles
    // ════════════════════════════════════════
    // Добавляются В ТОТ ЖЕ Map!
    'feeling': 0.5,      // T/F axis (приоритет гармонии)
    'perceiving': 0.3,   // J/P axis (гибкие границы)
  },
),
```

**Почему это работает:**
- `SummaryService` обрабатывает ключ `friendship_red_flags_v1:q1`
- Видит ВСЕ веса в `axisWeights` (и hierarchical, и bipolar)
- Summary Screen корректно отображает влияние на все шкалы
- Тот же подход используется во ВСЕХ тестах (DISC, Attachment Styles, Values & Priorities)

---

## 📚 Примеры из реальных тестов

### Пример 1: DISC Personality Test

```dart
'disc_personality_v1:q6': QuestionWeight(
  testId: 'disc_personality_v1',
  questionId: 'q6',
  axisWeights: {
    'enthusiasm': 1.3,
    'sociability': 1.2,
    'optimism': 1.0,
    'expressiveness': 1.0,
    // Bipolar weights в том же Map
    'extraversion': 1.0,  // Влияет на E/I
  },
),
```

### Пример 2: Attachment Styles Test

```dart
'attachment_styles_v1:q1': QuestionWeight(
  testId: 'attachment_styles_v1',
  questionId: 'q1',
  axisWeights: {
    'emotional_openness': 1.0,
    'vulnerability_sharing': 0.9,
    'secure_attachment': 0.9,
    // Bipolar weights
    'extraversion': 0.7,  // E/I: открытость в общении
    'feeling': 0.8,       // T/F: эмоциональная открытость
  },
),
```

### Пример 3: Values & Priorities Test

```dart
'values_priorities_v1:q17': QuestionWeight(
  testId: 'values_priorities_v1',
  questionId: 'q17',
  axisWeights: {
    'intimacy': 1.0,
    'emotional_intimacy': 1.0,
    'warmth': 0.8,
    // Bipolar weights
    'extraversion': 0.6,  // Социальный аспект
    'feeling': 0.8,       // Эмоциональная связь
  },
),
```

---

## 🛡️ Как избежать ошибки

### 1. ✅ Используйте validation script

```bash
bash tools/validate_test.sh your_test_id
```

**CHECK D1** обнаружит неправильный формат:
```
[CHECK D1] Проверка формата биполярных весов
✗ FAIL: Найдены QuestionWeight с суффиксом :personality (неправильный формат!)
ℹ INFO: ❌ НЕПРАВИЛЬНО: 'test_id:q1:personality': QuestionWeight(...)
ℹ INFO: ✅ ПРАВИЛЬНО: Добавьте биполярные веса В ТОТ ЖЕ axisWeights
ℹ INFO: Пример в ADDING_TEST_EXAMPLES.md строки 437-507
```

### 2. ✅ Читайте документацию ПЕРЕД началом

**Обязательно прочитайте:**
- `docs/adding-new-test/ADDING_TEST_RULES.md` - ПРАВИЛО #2, раздел "ФОРМАТ биполярных весов"
- `docs/adding-new-test/ADDING_TEST_EXAMPLES.md` - строки 437-507 (примеры)

### 3. ✅ Используйте примеры как шаблоны

Копируйте структуру из существующих тестов:
- `lib/config/summary/question_weights/disc_personality_weights.dart`
- `lib/config/summary/question_weights/attachment_style_weights.dart`
- `lib/config/summary/question_weights/values_priorities_weights.dart`

### 4. ✅ Проверяйте в приложении

После добавления весов:
1. Запустите тест: `flutter run`
2. Пройдите тест с **экстремальными ответами**:
   - Все "Strongly Agree" → должно влиять на одни полюса
   - Все "Strongly Disagree" → должно влиять на противоположные полюса
3. Откройте **Summary Screen** → проверьте влияние на 4 биполярные шкалы:
   - Extraversion ↔ Introversion
   - Sensing ↔ Intuition
   - Thinking ↔ Feeling
   - Judging ↔ Perceiving

**Если НЕ видите изменений** → формат весов неправильный!

---

## 🔍 Технические детали

### Как SummaryService обрабатывает веса

```dart
// summary_service.dart (~строка 250)
for (final answer in answers) {
  // Формирует ключ: "test_id:qN"
  final key = '${answer.testId}:${answer.questionId}';

  // Ищет вес по этому ключу
  final weight = _weights[key];

  if (weight != null) {
    // Итерирует по ВСЕМ шкалам в axisWeights
    weight.axisWeights.forEach((axisId, value) {
      // Обрабатывает и hierarchical, и bipolar шкалы
      if (bipolarPoles.contains(axisId)) {
        // Биполярная шкала (E/I, S/N, T/F, J/P)
      } else {
        // Hierarchical шкала (195 психологических)
      }
    });
  }
}
```

**Ключевой момент:** Ключ формируется как `test_id:qN`, **БЕЗ** суффикса `:personality`!

---

## 📊 Статистика ошибки

**Ошибка встречалась:**
- ✅ 1 раз обнаружена: Friendship Red Flags Test (2025-12-02)
- ✅ Исправлена немедленно
- ✅ Добавлена защита (CHECK D1 в validate_test.sh)
- ✅ Обновлена документация (ADDING_TEST_RULES.md)

**Причина возникновения:**
- Неочевидный формат (биполярные веса НЕ в отдельном объекте)
- Отсутствие явного упоминания в кратких инструкциях
- Примеры были в документации, но НЕ в начале файла

**Защита от повторения:**
- ✅ Validation script (CHECK D1)
- ✅ Явное правило в ADDING_TEST_RULES.md
- ✅ Этот гайд (BIPOLAR_WEIGHTS_GUIDE.md)

---

## 🎯 Checklist перед коммитом

Перед добавлением теста с биполярными весами:

- [ ] Биполярные веса добавлены **В ТОТ ЖЕ** `axisWeights` (НЕ отдельно!)
- [ ] НЕТ ключей с суффиксом `:personality`
- [ ] Запущен `bash tools/validate_test.sh test_id` (CHECK D1 пройден)
- [ ] Тест пройден в приложении с экстремальными ответами
- [ ] Summary Screen показывает влияние на 4 биполярные оси
- [ ] `flutter analyze` проходит без ошибок

---

**Последнее обновление:** 2025-12-02
**Версия:** 1.0.0
**Автор:** Claude Code (после обнаружения ошибки в Friendship Red Flags Test)
