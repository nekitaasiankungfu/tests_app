# ⚠️ ADDING TEST - 8 КРИТИЧЕСКИХ ПРАВИЛ

> **ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ!**
> Эти правила предотвратят 80% возможных ошибок при добавлении нового теста.

---

## 🔴 ПРАВИЛО #1: Scores ДОЛЖНЫ начинаться с 0 (НЕ с 1!)

### Проблема
Если использовать scores от 1, минимальный балл будет НЕ 0%, а проценты будут превышать 100%.

### ❌ НЕПРАВИЛЬНО:
```dart
// 5-point Likert (1-5) - минимум 20%, нормализация сломана
AnswerModel(text: {'ru': 'Никогда'}, score: 1),  // минимум = 20%
AnswerModel(text: {'ru': 'Всегда'}, score: 5),   // даст >100%!

// 6-point Likert (1-6) - ещё хуже, максимум = 120%!
AnswerModel(text: {'ru': 'Совершенно не согласен'}, score: 1),
AnswerModel(text: {'ru': 'Полностью согласен'}, score: 6),  // 6/5 = 120%!
```

### ✅ ПРАВИЛЬНО:
```dart
// 5-point Likert (0-4)
AnswerModel(text: {'ru': 'Никогда'}, score: 0),  // минимум = 0%
AnswerModel(text: {'ru': 'Всегда'}, score: 4),   // максимум = 100%

// 6-point Likert (0-5) - для тестов с 6 вариантами ответов
AnswerModel(text: {'ru': 'Совершенно не согласен'}, score: 0),
AnswerModel(text: {'ru': 'Не согласен'}, score: 1),
AnswerModel(text: {'ru': 'Скорее не согласен'}, score: 2),
AnswerModel(text: {'ru': 'Скорее согласен'}, score: 3),
AnswerModel(text: {'ru': 'Согласен'}, score: 4),
AnswerModel(text: {'ru': 'Полностью согласен'}, score: 5),  // максимум = 100%
```

### Формула
**Scores = 0 до (N-1)**, где N = количество вариантов ответа

| Вариантов | Scores | maxQuestionScore |
|-----------|--------|------------------|
| 4 | 0-3 | 3 |
| 5 | 0-4 | 4 |
| 6 | 0-5 | 5 |
| 7 | 0-6 | 6 |

### Математика
- Scores 1-5: `54 × 1 = 54` → `54/270 = 20%` ❌
- Scores 0-4: `54 × 0 = 0` → `0/216 = 0%` ✅
- Scores 1-6: `6/5 = 120%` ❌ (превышает 100%!)
- Scores 0-5: `5/5 = 100%` ✅

---

## 🔴 ПРАВИЛО #2: Отрицательные веса - когда и как использовать

### Таблица правил

| Тип шкалы | Количество | Отрицательные веса | Что делать |
|-----------|-----------|-------------------|------------|
| **195 психологических шкал** | 195 | ✅ **ДА** | Используйте для обратных корреляций |
| **8 биполярных полюсов** | 8 | ❌ **НЕТ** | Используйте противоположный полюс |

### 8 биполярных полюсов Personality Type (НЕ используйте отрицательные!)

- `extraversion` ↔ `introversion`
- `sensing` ↔ `intuition`
- `thinking` ↔ `feeling`
- `judging` ↔ `perceiving`

### Правило инверсии
- ❌ `'extraversion': -0.3` → ✅ `'introversion': 0.3`
- ❌ `'thinking': -0.5` → ✅ `'feeling': 0.5`

### Когда использовать отрицательные (195 психологических шкал)

```dart
// ✅ ПРАВИЛЬНО - обратная корреляция
'vitality': -1.0,           // Усталость снижает энергию
'stress_tolerance': -0.8,   // Стресс снижает толерантность
'anxiety': 0.9,             // Стресс повышает тревогу (прямая)
```

### ⚠️ Правило "(reversed)"
Все шкалы с комментариями "(reversed)" ДОЛЖНЫ иметь отрицательные веса!

### 🔴 ФОРМАТ биполярных весов: ВСТРАИВАТЬ в основной QuestionWeight!

**❌ НЕПРАВИЛЬНО** (создавать отдельные QuestionWeight с суффиксом `:personality`):
```dart
// ❌ Это НЕ РАБОТАЕТ! Summary Screen не увидит эти веса!
'friendship_red_flags_v1:q1': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    'boundary_setting': -1.0,
    'assertiveness': -0.9,
  },
),

// ❌ Отдельный QuestionWeight - НЕПРАВИЛЬНО!
'friendship_red_flags_v1:q1:personality': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    'feeling': 0.5,
    'perceiving': 0.3,
  },
),
```

**✅ ПРАВИЛЬНО** (биполярные веса в ТОМ ЖЕ axisWeights):
```dart
// ✅ Hierarchical И биполярные веса ВМЕСТЕ!
'friendship_red_flags_v1:q1': QuestionWeight(
  testId: 'friendship_red_flags_v1',
  questionId: 'q1',
  axisWeights: {
    // 195 hierarchical scales
    'boundary_setting': -1.0,
    'assertiveness': -0.9,
    'self_esteem': -0.7,
    // 8 bipolar poles (в том же Map!)
    'feeling': 0.5,  // Personality type
    'perceiving': 0.3,  // Personality type
  },
),
```

**Примеры в документации:** `ADDING_TEST_EXAMPLES.md` строки 437-507

---

## 🔴 ПРАВИЛО #3: Автоопределение direction по знаку веса

### Автоматическое определение
Direction определяется АВТОМАТИЧЕСКИ по знаку веса!

### ✅ ПРАВИЛЬНО:
```dart
'stress_test:q5': QuestionWeight(
  axisWeights: {
    'vitality': -1.0,         // Автоматически direction = -1
    'anxiety': 0.9,           // Автоматически direction = 1
  },
),
```

### ❌ НЕ НУЖНО:
```dart
axisDirections: {  // ← НЕ НУЖНО! Определится автоматически
  'vitality': -1,
},
```

---

## 🔴 ПРАВИЛО #4: summary_service.dart ДОЛЖЕН импортировать веса

### Проблема
Без импорта Personality Type шкалы не покажут влияние теста.

### Решение
В `lib/services/summary_service.dart`:

```dart
// 1. Импорт в начале файла
import '../config/summary/question_weights/your_test_weights.dart';

// 2. В методе getQuestionsWithPersonalityTypeWeights()
final allWeights = <String, QuestionWeight>{
  ...IPIPBigFiveWeights.weights,
  ...YourTestWeights.weights,  // ← ДОБАВИТЬ
};
```

---

## 🔴 ПРАВИЛО #5: Защита от NaN в расчетах

### Где добавлять
В Data файле (`getFactorInterpretation`):

```dart
static Map<String, String> getFactorInterpretation(String factorId, double percentage) {
  // Защита от NaN
  if (percentage.isNaN || percentage.isInfinite) {
    percentage = 0.0;
  }
  percentage = percentage.clamp(0.0, 100.0);
  // ... остальная логика
}
```

### Симптом
Ошибка "Converting object to an encodable object failed: NaN"

---

## 🔴 ПРАВИЛО #6: Используйте ТОЛЬКО существующие 195 шкал

### Проблема придуманных шкал
- ❌ Шкала НЕ появится в Summary
- ❌ Вес проигнорируется
- ❌ Ложное покрытие теста

### ❌ НЕПРАВИЛЬНО:
```dart
'social_battery_v1:q1': QuestionWeight(
  axisWeights: {
    'social_energy_depletion': 1.0,  // ❌ НЕ существует!
    'interaction_fatigue': 0.9,       // ❌ НЕ существует!
  },
)
```

### ✅ ПРАВИЛЬНО:
```dart
'social_battery_v1:q1': QuestionWeight(
  axisWeights: {
    'vitality': -0.9,                 // ✅ существует
    'stress_tolerance': -0.7,         // ✅ существует
    'introversion': 0.8,              // ✅ существует
  },
)
```

### Как проверить
1. Откройте `lib/config/summary/hierarchical_scales.dart`
2. Используйте Ctrl+F для поиска шкалы
3. Если не найдена - выберите семантически близкую

### Доступные категории (195 шкал)
- **Эмоциональная** (~40): anxiety, stress_tolerance, emotional_resilience...
- **Социальная** (~35): extraversion, sociability, empathy...
- **Когнитивная** (~30): creativity, problem_solving, attention_control...
- **Мотивационная** (~25): achievement_striving, persistence...
- **Личностные** (~30): conscientiousness, openness, impulsiveness...
- **Отношения** (~25): trust, intimacy, commitment...
- **Самооценка** (~20): self_esteem, self_compassion...

---

## 🔴 ПРАВИЛО #7: Уникальные шкалы теста vs Summary шкалы

### ⚠️ КРИТИЧНО: FactorModel - УСТАРЕВШИЙ API!

### ❌ НЕ ИСПОЛЬЗУЙТЕ:
```dart
static List<FactorModel> get _factors => [...];  // ❌ FactorModel не существует!
```

### ✅ ИСПОЛЬЗУЙТЕ:
```dart
// Для уникальных факторов теста
static Map<String, Map<String, String>> getFactorNames() {
  return {
    'emotional_exhaustion': {
      'ru': 'Эмоциональное истощение',
      'en': 'Emotional Exhaustion',
    },
  };
}

static Map<String, String> getFactorInterpretation(String factorId, double percentage) {
  // NaN protection (ПРАВИЛО #5)
  if (percentage.isNaN) percentage = 0.0;
  // ... интерпретации
}
```

### Два типа шкал

1. **Уникальные факторы теста**
   - Только для результатов теста
   - Через getFactorNames() и getFactorInterpretation()
   - НЕ добавлять в hierarchical_scales.dart

2. **Summary шкалы (weights)**
   - 195 психологических + 8 биполярных
   - ДОЛЖНЫ существовать в hierarchical_scales.dart
   - Используются в weights файле

### Пример маппинга
```dart
// Фактор теста "emotional_exhaustion" маппится на:
'vitality': -1.0,               // Существующая шкала
'emotional_resilience': -0.9,   // Существующая шкала
'burnout': 0.9,                 // Существующая шкала
// НЕ 'emotional_exhaustion': 1.0 - это фактор теста!
```

---

## 🔴 ПРАВИЛО #8: maxQuestionScore КРИТИЧЕН для правильных процентов

### Проблема
Неправильный maxQuestionScore → максимальный ответ показывает НЕ 100%.

### Таблица

| Шкала ответов | Варианты | Scores | maxQuestionScore | Примеры |
|--------------|----------|--------|------------------|---------|
| Likert 4-point | 4 | 0-3 | **3** | Fisher Temperament |
| Likert 5-point | 5 | 0-4 | **4** | DISC, Holland Code, Love Languages |
| Likert 6-point | 6 | 0-5 | **5** | Big Five, Wellbeing Happiness Inventory |

### ⚠️ ВАЖНО: maxQuestionScore = максимальный score, НЕ количество вариантов!

```dart
// 6 вариантов ответа со scores 0-5
maxQuestionScore = 5  // ✅ (максимальный score)
maxQuestionScore = 6  // ❌ (это количество вариантов, не максимальный score!)
```

### Как добавить

В `lib/services/test_service.dart` (~строка 92):

```dart
final int maxQuestionScore = test.id == 'fisher_temperament'
    ? 3   // 4 варианта, scores 0-3
    : test.id == 'anxiety_symptoms_inventory_v1'
        ? 3   // 4 варианта, scores 0-3
        : test.id == 'disc_personality_v1'
            ? 4   // 5 вариантов, scores 0-4
            : test.id == 'holland_code_v1'
                ? 4   // 5 вариантов, scores 0-4
                : test.id == 'love_languages_v1'
                    ? 4   // 5 вариантов, scores 0-4
                    : test.id == 'wellbeing_happiness_inventory_v1'
                        ? 5   // 6 вариантов, scores 0-5
                        : 5;  // Default (6 вариантов, scores 0-5)
```

### Проверка
- Пройдите тест с МАКСИМАЛЬНЫМИ ответами
- Должно показывать 100%
- Если >100% → scores начинаются с 1, а не с 0 (см. ПРАВИЛО #1)
- Если <100% → maxQuestionScore слишком большой

---

## 📋 Критический чеклист перед коммитом

```markdown
□ Все scores начинаются с 0 (0-4 для 5 вариантов, 0-5 для 6 вариантов)
□ maxQuestionScore = максимальный score (НЕ количество вариантов!)
□ Personality Type веса только положительные
□ Комментарии "(reversed)" имеют отрицательные веса
□ Используются ТОЛЬКО существующие 195 шкал
□ НЕ используется FactorModel
□ summary_service.dart импортирует веса
□ getFactorInterpretation() имеет NaN protection
□ flutter analyze без ошибок
□ Тест показывает 0-100% (не 20-100% и не >100%)
```

---

## 🚨 Автоматическая проверка

```bash
bash tools/validate_test.sh your_test_id
```

Проверяет:
- Валидность шкал
- Дубликаты ключей
- Регистрацию во всех точках
- Отрицательные веса на биполярных полюсах

---

> **💡 Главное:** Следуйте этим 8 правилам и используйте чеклист - это предотвратит большинство ошибок!